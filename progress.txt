# CakeCraft — Progress Log
# Этот файл используется как память между сессиями Ralph Loop.
# Каждая сессия ДОБАВЛЯЕТ (append) записи, не перезаписывает файл.
# Формат: [дата] [task_id] — что сделано, что узнал, на что обратить внимание.

---
[INIT] Проект CakeCraft — интерактивное меню-конструктор для кондитеров.
[INIT] Стек: React + Vite + TypeScript + Tailwind CSS + Supabase + Telegram Bot API.
[INIT] Архитектура: один деплой, много кондитеров. Каждый кондитер получает уникальный URL по slug.
[INIT] Клиентская часть: 6-шаговый конструктор торта с фото-карточками и авторасчётом цены.
[INIT] Админка: редактор меню, заказы, профиль.
[INIT] Уведомления: Telegram-бот + email (email — фаза V1).
[INIT] Всего 25 задач. Критических — 16. Порядок: инфра → БД → типы → layout → экраны → админка → бот → тесты → деплой.

[2026-03-06] [task-1] Инициализирован проект React + Vite + TypeScript + Tailwind CSS. Добавлены базовый роутинг через react-router-dom, строгий TypeScript (strict=true), конфиги ESLint и Prettier, создана структура папок src/components, src/pages, src/lib, src/types, src/hooks, src/assets. Проверки: `npm run build`, `npm run lint`, `npm run format` — успешно; `npm run dev` стартует корректно (Vite ready на http://127.0.0.1:4173/).
[2026-03-06] [task-2] Подключён Supabase SDK (`@supabase/supabase-js`), создан клиент в `src/lib/supabase.ts` с env-переменными `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`, добавлен `.env.example`, обновлён `.gitignore` для `.env`. Добавлен скрипт проверки подключения `npm run supabase:check` (`scripts/check-supabase.mjs`) через запрос к `/auth/v1/health`. Проверки: `npm run lint` и `npm run build` — успешно; `npm run supabase:check` требует реальные env-переменные и в текущем окружении завершился с сообщением об их отсутствии.
[2026-03-06] [task-3] Добавлена SQL-миграция `supabase/migrations/20260306130430_create_bakers_table.sql`: создана таблица `public.bakers` со всеми полями из ТЗ, включён RLS, добавлены политики публичного чтения и полного доступа владельца по `telegram_id`, настроен trigger `set_bakers_updated_at` через функцию `public.set_updated_at()` для автообновления `updated_at`. Проверки: `npm run lint` и `npm run build` — успешно.
[2026-03-06] [task-4] Добавлена SQL-миграция `supabase/migrations/20260306130601_create_menu_items_table.sql`: создана таблица `public.menu_items` со всеми полями, FK `baker_id -> bakers.id` с `ON DELETE CASCADE`, CHECK-ограничения для `category` и `price_type`, индекс `(baker_id, category, sort_order)`, включён и принудительно применён RLS, добавлены политики публичного чтения только активных позиций и полного доступа владельца через `telegram_id` из JWT. Проверки: `npm run lint` и `npm run build` — успешно; `npm test` недоступен (отсутствует script `test`).
[2026-03-06] [task-5] Добавлена SQL-миграция `supabase/migrations/20260306130730_create_orders_table.sql`: создана таблица `public.orders` со всеми полями из ТЗ, добавлены FK (`baker_id -> bakers.id`, `filling_id/coating_id -> menu_items.id`), CHECK-ограничения для `status`, `delivery_type`, `client_contact_type`, индексы `(baker_id, status)` и `(baker_id, order_date)`. Также включён RLS с политиками на создание заказа и доступ владельца по `telegram_id` через JWT. Проверки: `npm run lint` и `npm run build` — успешно; `npm test` недоступен (в `package.json` отсутствует script `test`).
[2026-03-06] [task-7] Добавлена SQL-миграция `supabase/migrations/20260306130840_create_photos_storage_bucket.sql`: создан/обновлён bucket `photos` с публичным чтением, лимитом файла 5MB и разрешёнными MIME-типами `image/jpeg`, `image/png`, `image/webp`. Добавлены политики для `storage.objects`: публичное чтение из bucket `photos` и загрузка только в подпапки `menu/`, `references/`, `gallery/` с ограничениями по MIME-типу и размеру. Проверки: `npm run lint` и `npm run build` — успешно; `npm test` недоступен (в `package.json` отсутствует script `test`).
